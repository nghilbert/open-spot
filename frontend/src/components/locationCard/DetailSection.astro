---
import DetailPill from "./DetailPill.astro";

interface Props {
  details: any[];
}

const { details } = Astro.props as Props;
---

<div class="d-flex flex-wrap gap-2">
  {details.map((detail) => (
    <DetailPill detail={detail} />
  ))}
</div>