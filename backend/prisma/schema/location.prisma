enum Type {
  PARKING_LOT   
  Bike_RACK
  NONE
}

model Location {
  id Int @id @default(autoincrement())
  address String @unique
  name String
  spotCapacity Int
  spotAvailability Int
  type Type @default(NONE)
}