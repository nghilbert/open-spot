model Password {
  passwordId String @id @default(cuid())  
  passwordHash String
  createdOn DateTime @default(now())

  user User? @relation("CurrentPassword")
  userOld User[] @relation("OldPasswords")
}